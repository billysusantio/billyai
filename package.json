{
  "name": "billyai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BillyAI — a WhatsApp clone of Billy that chats with his team and sends per-role / per-team reminders. Fonnte + Anthropic Claude + Railway.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^3.0.3"
  }
}
